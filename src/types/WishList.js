/**
 * @typedef {Object} WishList
 * @property {number} id
 * @property {number} user_id
 * @property {number} product_id
 * @property {number|null} blindbox_id
 * @property {Date} created_at
 */

// Export is not needed in JSDoc approach but can be kept for module structure
export {}
